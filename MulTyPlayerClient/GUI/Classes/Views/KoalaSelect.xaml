<UserControl x:Class="MulTyPlayerClient.GUI.Views.KoalaSelect"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:MulTyPlayerClient.GUI"
        xmlns:viewmodels ="clr-namespace:MulTyPlayerClient.GUI.ViewModels"
        xmlns:views ="clr-namespace:MulTyPlayerClient.GUI.Views"
        mc:Ignorable="d"
        d:DesignHeight="400" d:DesignWidth="600">
    <UserControl.DataContext>
        <viewmodels:KoalaSelectViewModel/>
    </UserControl.DataContext>
    <Grid>
        <!--Background video-->
        <MediaElement Stretch="UniformToFill">
            <MediaElement.Triggers>
                <EventTrigger RoutedEvent="MediaElement.Loaded">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <!-- The MediaTimeline has a RepeatBehavior="Forever" which makes the media play
                                over and over indefinitely.-->
                                <MediaTimeline RepeatBehavior="Forever"
                                               Source="pack://siteoforigin:,,,/GUI/KoalaSelectionAssets/Background.mp4"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
            </MediaElement.Triggers>
        </MediaElement>

        <!--Koala grid-->
        <Grid Margin="5 10 5 30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ContentControl Content="{Binding Boonie}"/>
            <ContentControl Content="{Binding Mim}" Grid.Column="1"/>
            <ContentControl Content="{Binding Gummy}" Grid.Column="2"/>
            <ContentControl Content="{Binding Snugs}" Grid.Column="3"/>
            <ContentControl Content="{Binding Katie}" Grid.Column="0" Grid.Row="1"/>
            <ContentControl Content="{Binding Kiki}" Grid.Column="1" Grid.Row="1"/>
            <ContentControl Content="{Binding Elizabeth}" Grid.Column="2" Grid.Row="1"/>
            <ContentControl Content="{Binding Dubbo}" Grid.Column="3" Grid.Row="1"/>
<!--
            <views:KoalaSelectEntry DataContext="{Binding Boonie}"/>
            <views:KoalaSelectEntry DataContext="{Binding Mim}"/> 
            <views:KoalaSelectEntry DataContext="{Binding Gummy}"/>
            <views:KoalaSelectEntry DataContext="{Binding Snugs}"/>        
            <views:KoalaSelectEntry DataContext="{Binding Katie}"/>        
            <views:KoalaSelectEntry DataContext="{Binding Kiki}"/>         
            <views:KoalaSelectEntry DataContext="{Binding Elizabeth}"/>    
            <views:KoalaSelectEntry DataContext="{Binding Dubbo}"/>
-->
        </Grid>

        <TextBlock Text="Select Your Koala" 
                   VerticalAlignment="Bottom"
                   Margin="0 12"/>

        <Border Background="Transparent"
                IsHitTestVisible="{Binding BlockKoalaSelect}"/>

    </Grid>

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewmodels:KoalaSelectEntryViewModel}">
            <views:KoalaSelectEntry/>
        </DataTemplate>
        <Style TargetType="TextBlock">
            <Setter Property="TextElement.FontFamily" Value="Fonts/#SF Slapstick Comic"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="0" 
                                      Direction="315" 
                                      ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    

    <UserControl.Background>
        <ImageBrush ImageSource="pack://siteoforigin:,,,/GUI/KoalaSelectionAssets/Background.jpg" Stretch="UniformToFill"/>
    </UserControl.Background>

</UserControl>
